# page-27

Source: https://react.dev/reference/react/useCallback.md

{items.map(item => {
// 🔴 You can't call useCallback in a loop like this:
const handleClick = useCallback(() => {
sendReport(item)
}, [item]);
return (

);
})}
